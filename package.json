{
  "name": "mesh-home-task",
  "version": "1.0.0",
  "private": true,
  "description": "Demo shop (Vite frontend + Express backend). Root package.json exists for Vercel: the serverless function in api/ resolves the backend's dependencies from the root node_modules.",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^9.0.1"
  }
}
